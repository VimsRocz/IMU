# Use the official VS Code Python devcontainer image
ARG VARIANT="3.9"
FROM mcr.microsoft.com/vscode/devcontainers/python:0-${VARIANT}

WORKDIR /workspace
# (Add any apt-get installs here if your old setup needed them.)
